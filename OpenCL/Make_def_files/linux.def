# Use default cc compiler on Linux
# copy to make.def
CC        = cc
CLINKER   = $(CC)
OPTFLAGS  = -fopenmp -O3
LIBS      = -lm
PRE       = ./

CFLAGS	  = $(OPTFLAGS)
OCL_LIBS  = -lOpenCL

OBJ=o
EXE=
RM=rm -f
